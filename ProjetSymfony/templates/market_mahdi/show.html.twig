{% extends 'base.html.twig' %}

{% block title %}Market{% endblock %}

{% block body %}
    <h1>Market</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ market.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ market.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ market.description }}</td>
            </tr>
            <tr>
                <th>Validdate</th>
                <td>{{ market.validdate ? market.validdate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_market_mahdi_index') }}">back to list</a>

    <a href="{{ path('app_market_mahdi_edit', {'id': market.id}) }}">edit</a>

    {{ include('market_mahdi/_delete_form.html.twig') }}
{% endblock %}
